[project]
name = "public-distribution"
version = "0.1.0"
description = ""
authors = [
    {name = "Joyal V Poulose"}
]
requires-python = ">=3.14"
dependencies = [
    "flask (>=3.1.3,<4.0.0)",
    "oracledb (>=4.0.2,<5.0.0)"
]

[tool.poetry]
package-mode = false

[dependency-groups]
dev = [
    "jinja-lsp (>=0.4.1,<0.5.0)"
]

[tool.jinja-lsp]
templates = "backend/templates"
backend = ["backend"]
